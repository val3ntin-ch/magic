import Head from "next/head";

const Fallback = () => (
  <>
    <Head>
      <title>pwa example with magic</title>
    </Head>
    <h1>Internet connection it is off </h1>
  </>
);

export default Fallback;
